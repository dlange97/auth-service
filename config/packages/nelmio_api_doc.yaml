nelmio_api_doc:
  documentation:
    info:
      title: Auth Service API
      description: Authentication, user management, roles, and auth-related health endpoints.
      version: 1.0.0
    components:
      securitySchemes:
        bearerAuth:
          type: http
          scheme: bearer
          bearerFormat: JWT
    security:
      - bearerAuth: []
  areas:
    default:
      path_patterns:
        - ^/auth
